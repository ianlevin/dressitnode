const config = {
    user        : "DressIt", // Database username
    password    : "aa", // Database password
    server      : "localhost", // Server IP address
    database    : "DressIt", 
    port        : 1433,
    options: {
        encrypt: false,
        trustServerCertificate: true,
        }
}

export default config;